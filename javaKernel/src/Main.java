public class Main {
    public static void main(String[] args) {
        /*
        try {
            ExcelFile sheetObjList = new ExcelFile("d:\\\\project\\\\IA_TIALibV16\\\\proj\\\\javaKernel\\\\Kernal.xlsx", 1, "1");
            for (int i = 1; i < 10; i++) {
                if (sheetObjList.isTypeEquals("XV", i)) {
                    sheetObjList.setParametersByObjectType("XV", i);
                }
                if (sheetObjList.isTypeEquals("A", i)) {
                    sheetObjList.setParametersByObjectType("A", i);
                }
                if (sheetObjList.isTypeEquals("S", i)) {
                    sheetObjList.setParametersByObjectType("S", i);
                }
                if (sheetObjList.isTypeEquals("MR", i)) {
                    sheetObjList.setParametersByObjectType("MR", i);
                }
                if (sheetObjList.isTypeEquals("M", i)) {
                    sheetObjList.setParametersByObjectType("M", i);
                }

            }
            sheetObjList.save();
        }
        catch (Exception e) {
            //Do something.
        }

         */
        Test a = new Test();
        a.foo();
    }
}
